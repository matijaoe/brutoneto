{
  "name": "@brutoneto/api",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @brutoneto/core run build && nitro build",
    "dev": "nitro dev --port 4000",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs",
    "bump": "bumpp",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "eslint . --fix"
  },
  "dependencies": {
    "@brutoneto/core": "workspace:*"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^7.2.0",
    "eslint": "^9.39.2",
    "nitropack": "^2.13.1",
    "zod": "^4.3.6"
  }
}
